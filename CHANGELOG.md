# Changelog

本项目的主要版本变更记录在此文件中。

## [1.3.0] - 2026-08-08

### Added

- 新增本地 Todo 功能模块，支持任务创建、编辑、删除、快速录入与四种状态切换。
- Todo 支持独立标签体系、按状态和标签筛选、标签重命名与删除，以及高优先级标记。

### Changed

- 替换应用明暗主题，统一颜色、字体、圆角、阴影及侧边栏等视觉样式。
- 优化书签与 Todo 的点击区域、右键菜单和交互反馈，并拆分 Todo 页面组件以提升可维护性。
- 在导航栏服务状态控件中展示本地服务地址。
- 优化笔记文件变更处理和前端构建告警输出。

## [1.2.0] - 2026-08-08

### Added

- 书签侧边栏增加标签搜索，以及“全部 / 星标”视图切换。

### Changed

- 默认展示全部书签；文本搜索和标签筛选继续沿用原有查询行为。
- 标签查询接口增加查询词和数量限制参数，侧边栏默认展示书签数最多的前 50 个标签。

## [1.1.1] - 2026-08-08

### Changed

- 将全局输入框圆角调整为 12px。

## [1.1.0] - 2026-08-07

### Added

- 为书签增加星标能力，默认列表仅展示星标书签，并按星标时间倒序排列。
- 支持在书签卡片上添加或取消星标；默认列表取消星标前会请求确认。
- JSON 导入导出增加 `starred_at` 字段，并兼容缺少该字段的旧备份。

### Changed

- 将星标列表设为显式查询模式，保持 HTTP API 空查询返回最近书签的既有语义。
- 星标导出时间保留毫秒精度，确保导入后排序稳定。
- 清空搜索框并失焦时自动恢复默认星标列表，无需再次按回车搜索。
- 星标操作期间禁用对应按钮；失败时显示错误提示，成功后刷新书签列表。

### Removed

- 删除已执行完毕的一次性标签合并 SQL、验证脚本及相关审计和执行文档。
- 删除已过期的 BKMR 到 SQLite 一次性迁移及回滚手册。
