# Changelog

本项目的主要版本变更记录在此文件中。

## [1.15.0] - 2026-08-26

### Changed

- 拆分书签、Todo 与 RSS 页面中的查询、交互编排和展示职责，降低页面组件复杂度。
- TanStack Query 增加 30 秒数据新鲜期，并统一 Todo 与 RSS 的领域缓存失效及分页缓存更新规则。
- 将本地书签的 `claude` 标签合并到 `claude-code`，统一相关书签分类。

### Fixed

- Todo 写入成功后统一等待后端 `todos-changed` 事件刷新缓存，避免 mutation 回调与事件造成重复查询。
- RSS 标记已读及订阅刷新失败时增加错误 toast，同时保持正在阅读的文章不会从未读列表中突然消失。

## [1.14.0] - 2026-08-24

### Added

- 通用设置新增 Todo 默认导出目录，并在 Todo Markdown 导出保存对话框中自动使用；未配置时仍回退为仅提供默认文件名。
- 新增统一的行内目录设置组件，支持路径浏览、手动输入、Enter 保存、Escape 取消、保存状态和行内错误反馈。

### Changed

- 设置页精简为“通用、服务、关于”三个分类；书签导出目录、Todo 导出目录与 Obsidian 笔记目录集中到通用设置，书签 JSON 导入/导出保留为独立数据操作区域。
- RSSHub 与小牛翻译统一归入服务管理界面，按内容服务和翻译服务分组，并在切换服务时保留各自未保存的草稿。
- 设置文件重组为 `common.paths` 与 `services` 两个领域分组，RSS 与笔记运行时同步读取新配置位置。
- Todo 导出与书签导出共用目录和文件名拼接工具；Todo 标签名中的路径分隔符及非法文件名字符会被净化。
- 拆分 Todo 导出与标签归档删除的前端编排逻辑，缩减 Todo 页面职责。

### Fixed

- Todo 查询结果为空时拒绝导出，不再创建空文件并错误提示导出成功。
- 归档删除支持展示 Tauri 返回的结构化后端错误，并避免同时出现弹窗错误与重复 toast。
- 文件导出错误改为向界面返回稳定提示，同时在内部记录详细写入和临时文件清理错误。

## [1.13.0] - 2026-08-24

### Added

- Todo 分类标签右键菜单新增“导出”：将当前分类下全部状态的待办导出为 Markdown 文档，文件名格式为“日期-待办-标签名.md”，进行中、已完成、已挂起、已取消四种状态分别以任务列表、复选+完成日期、中文前缀、删除线等语法渲染。
- Todo 分类标签右键菜单新增“归档删除”：删除标签及其下所有待办任务；标签下存在进行中待办时拒绝删除，并提示“当前标签存在未完成待办，无法归档删除。”。

### Changed

- Todo 分类标签右键菜单原“删除”更名为“标签删除”，仅删除标签及关联、不删除任何待办，行为保持不变。
- 抽取共享原子写工具，统一书签导出与 Todo 导出的临时文件写入与失败清理。
- Todo 导出与书签导出的文件路径展示统一使用公共路径格式化函数，长路径折叠为“前三级…后三级”，与设置中的目录展示一致。

## [1.12.0] - 2026-08-24

### Changed

- 服务设置改为默认查看、按需编辑，查看状态下对 API Key 脱敏，并在重置未保存内容前增加确认弹窗。
- 笔记目录和书签备份目录在查看状态下仅展示前三级与后三级路径，编辑时仍保留完整路径。

### Fixed

- 修复 API Key 和 RSS Access Key 的可见性按钮在按下时因定位 transform 被覆盖而向下跳动的问题。

## [1.11.0] - 2026-08-22

### Added

- 设置页增加“服务”分类和小牛翻译配置面板，可在应用内保存 App ID 与 API Key，并在保存后立即用于描述翻译。

### Changed

- 翻译服务改为从应用数据目录的 `settings.json` 动态读取凭据，不再依赖环境变量或编译期源码路径。
- 简化书签导入的 `starred_at` 数据模型；缺失值与 `null` 统一表示未加星，并统一内部错误类型从 `crate::error` 导入。

### Removed

- 移除 `.env.local` 加载逻辑、`dotenvy` 依赖，以及旧版扁平设置与书签星标三态数据的兼容代码。

## [1.10.0] - 2026-08-21

### Added

- 增加可替换的描述翻译 Provider 和本机 `POST /api/translations` 端点，支持通过小牛翻译自动翻译纯外文网页描述，第三方凭据仅保留在 Rust 进程环境中。
- Chrome 扩展增加描述翻译状态、失败提示、连接重试和标签输入交互测试。

### Changed

- 设置页改为顶部“通用、书签、笔记、RSS、关于”分类 Tab，并拆分为独立业务 Section；各分类独立管理编辑、保存、错误和未保存草稿状态。
- 书签与笔记目录支持系统目录选择，系统信息移动到“关于”并按需加载，RSS Access Key 保持默认脱敏。
- 统一本机 HTTP API 的参数拒绝和错误响应结构，并将运行时 `/api/docs` 作为当前接口契约入口。
- 优化扩展的书签查询、标签建议、描述自动增长和连接失败反馈。

### Fixed

- 修复设置页顶部 Tab 容器出现纵向滚动条，以及选中指示线被滚动容器裁剪的问题。

## [1.9.0] - 2026-08-21

### Added

- Chrome 扩展增加 Vite、Svelte 5、TypeScript 与 Vitest 工程化支持，并纳入 pnpm workspace 的统一开发、构建、检查和测试流程。
- 支持通过 `.env` 配置本地 API 地址；构建时会同步生成与 API origin 一致的 Manifest `host_permissions`。

### Changed

- Chrome 扩展更名为 `bkmrx-ext`，并将桌面端、Tauri 应用、Rust crate 和扩展版本统一更新到 1.9.0。
- 将扩展 popup 重构为 Svelte 组件，并拆分 API 客户端、Chrome API 适配层、书签表单模型和领域类型。
- 将 Tagify 改为 npm 依赖，生产构建统一输出到 `apps/chrome-extension/dist`。

### Removed

- 移除扩展原有的 Alpine.js 页面实现、仓库内压缩版第三方库，以及没有实际业务任务的 Manifest V3 background service worker。

## [1.8.0] - 2026-08-19

### Added

- 新增本地 RSS 阅读工作区，支持发现并订阅 RSS/Atom Feed、全部/未读/单订阅浏览、游标分页、正文阅读及已读状态管理。
- 支持订阅重命名、删除、单独刷新与批量刷新；进入 RSS 页面时自动刷新超过 15 分钟未更新的订阅。
- 增加 RSS 数据库 V2 迁移，持久化订阅、文章、刷新结果和已读状态。

### Changed

- 主导航增加 RSS 入口和 `Cmd/Ctrl+4` 快捷键，并采用订阅、文章列表、正文的固定三栏布局。
- 抽取书签预览与 RSS 共用的安全 HTTP 校验，限制协议、重定向、私网目标和响应大小；RSS 正文写入前会重写相对链接并清理不安全 HTML。

## [1.7.0] - 2026-08-18

### Added

- 书签侧栏增加“随便看看”视图，每次从全部书签中随机展示最多 7 条，并提供可重复抽取的骰子动画。
- 在数据库 baseline 中为书签最近更新时间排序增加 `(updated_at DESC, id DESC)` 复合索引。

### Changed

- 将书签列表请求改为 `browse`、`search`、`random` 判别联合，拒绝无效字段组合，同时保持现有 HTTP 列表接口兼容。
- 将访问统计从通用书签变更事件中分离，前端仅更新对应书签的访问次数和访问时间，避免重新拉取并扰动列表。
- 提取书签仓储与搜索共用的 SQL 占位符和 LIKE 转义工具。

### Fixed

- 随机结果在窗口重新聚焦、书签编辑、星标或删除后保持当前批次稳定，只在用户主动抽取时换一批。

## [1.6.0] - 2026-08-17

### Changed

- 将桌面端运行时升级到 React 与 React DOM 19.2.8，并同步采用与 TypeScript 5.5 兼容的 React 19 类型定义。
- 调整 React 19 的 JSX 返回类型与 `useRef` 初始化方式，保持笔记编辑与防抖保存行为不变。
- 将书签页改为标签侧栏与主内容区的两栏结构，并统一书签、笔记和 Todo 工作区的侧栏宽度、背景及主体表面样式。
- 将全局输入框圆角由 `rounded-xl` 收敛为 `rounded-md`。

### Fixed

- 修复 TagInput 获得焦点时建议面板因错误的 `focus-out` 判定而闪现后立即关闭的问题。

## [1.5.2] - 2026-08-17

### Added

- 增加共享删除确认框、统一的 Tauri 事件订阅 Hook，以及 Alert、Badge、Card、Checkbox、Empty、Field、Popover、Separator、Spinner 等基础 UI 组件。
- 增加书签表单、笔记工作区与设置页拆分后的独立组件和行为测试，覆盖异步事件清理、删除错误恢复、键盘操作与无障碍语义。
- 增加 schema V1 基线及迁移运行器测试，并保留重置前 V3 数据库的校验与备份说明。

### Changed

- 重构书签新增/编辑表单、笔记面板和设置页，分离数据编排与展示职责，统一加载、空状态、错误反馈和表单校验样式。
- 标签输入改用可访问的多选组合框与 Popover，支持完整键盘操作；Todo 标签筛选恢复按钮语义。
- 书签、笔记、Todo 和 Todo 标签统一使用二次删除确认，并避免前一次 mutation 错误泄漏到新目标。
- 将数据库基线版本从 V3 重置为 V1；现有 schema 2/3 数据库会被拒绝打开，需要使用备份或重新初始化后再运行。
- 将项目实际使用的 shadcn Tailwind data-state variants 本地化，CLI 改为按需运行。

### Removed

- 移除项目内的 shadcn CLI、MCP SDK 传递依赖，以及未使用的 CodeMirror 多语言包和主题依赖。

## [1.5.1] - 2026-08-14

### Added

- 笔记列表右键菜单增加重命名入口，并复用新建笔记弹窗编辑文件名。

### Changed

- 重命名当前打开的笔记后同步更新编辑器文件路径，并避免对未变化的文件名发起请求。

## [1.5.0] - 2026-08-14

### Added

- 增加自适应书签预览，由后端统一返回网页、GitHub 仓库摘要或失败兜底方案。
- GitHub 仓库改用 API 获取名称、描述、主要语言、Stars、Forks 和更新时间，并为后续注入 GitHub Key 预留独立客户端边界。
- 为禁止 iframe、网络超时、连接失败、HTTP 错误和 GitHub API 异常增加持续可见的预览错误卡与重试入口。

### Changed

- 普通网页在进入 iframe 前由后端检查重定向、`X-Frame-Options` 和 CSP，并增加短期缓存、请求超时及 SSRF 防护。
- 预览刷新与错误重试会绕过后端缓存；切换或关闭书签时忽略已经过期的异步响应。
- 笔记文件列表的选中背景改为与文件夹列表一致的玫红主题浅色。

## [1.4.0] - 2026-08-12

### Added

- 使用 TanStack Hotkeys 统一管理应用快捷键，支持通过 `Cmd/Ctrl+1`、`Cmd/Ctrl+2`、`Cmd/Ctrl+3` 切换书签、笔记和 Todo 工作区。
- 书签页面增加 Vim 风格键盘操作：`/` 聚焦搜索框，`J/K` 切换选中书签，`P` 打开预览，`X` 关闭预览，`O` 在系统浏览器中打开。

### Changed

- 区分书签的键盘选中和鼠标悬停样式，并在输入框、编辑器及弹窗交互期间停用单键快捷键。
- 设置页的备份目录和笔记目录改为就地编辑，默认完整展示路径，按需切换为输入框保存或取消。
- 为 JSON 导入和导出按钮增加方向图标。
- 将笔记编辑器已有快捷键迁移到 TanStack Hotkeys，并保持原有编辑、保存行为。

## [1.3.1] - 2026-08-12

### Added

- 书签卡片增加应用内网页预览，可在右侧 Sheet 中通过受限 iframe 浏览 HTTP(S) 链接。
- 网页预览提供加载状态、刷新、关闭和系统浏览器打开入口，并保留原有标题外部打开行为。

### Changed

- 非 HTTP(S) 书签改为直接交给系统处理；打开失败时显示提示且不阻断书签页面。
- 优化网页预览的焦点和动画时机，避免 Sheet 打开或关闭时引发主界面滚动与布局抖动。
- 增加网页预览、协议兜底、访问计数、焦点恢复和 iframe 权限的自动化测试。

## [1.3.0] - 2026-08-08

### Added

- 新增本地 Todo 功能模块，支持任务创建、编辑、删除、快速录入与四种状态切换。
- Todo 支持独立标签体系、按状态和标签筛选、标签重命名与删除，以及高优先级标记。

### Changed

- 替换应用明暗主题，统一颜色、字体、圆角、阴影及侧边栏等视觉样式。
- 优化书签与 Todo 的点击区域、右键菜单和交互反馈，并拆分 Todo 页面组件以提升可维护性。
- 在导航栏服务状态控件中展示本地服务地址。
- 优化笔记文件变更处理和前端构建告警输出。

## [1.2.0] - 2026-08-08

### Added

- 书签侧边栏增加标签搜索，以及“全部 / 星标”视图切换。

### Changed

- 默认展示全部书签；文本搜索和标签筛选继续沿用原有查询行为。
- 标签查询接口增加查询词和数量限制参数，侧边栏默认展示书签数最多的前 50 个标签。

## [1.1.1] - 2026-08-08

### Changed

- 将全局输入框圆角调整为 12px。

## [1.1.0] - 2026-08-07

### Added

- 为书签增加星标能力，默认列表仅展示星标书签，并按星标时间倒序排列。
- 支持在书签卡片上添加或取消星标；默认列表取消星标前会请求确认。
- JSON 导入导出增加 `starred_at` 字段，并兼容缺少该字段的旧备份。

### Changed

- 将星标列表设为显式查询模式，保持 HTTP API 空查询返回最近书签的既有语义。
- 星标导出时间保留毫秒精度，确保导入后排序稳定。
- 清空搜索框并失焦时自动恢复默认星标列表，无需再次按回车搜索。
- 星标操作期间禁用对应按钮；失败时显示错误提示，成功后刷新书签列表。

### Removed

- 删除已执行完毕的一次性标签合并 SQL、验证脚本及相关审计和执行文档。
- 删除已过期的 BKMR 到 SQLite 一次性迁移及回滚手册。
